{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2022",                  // Modern JS output compatible with Node 20
    "module": "NodeNext",                  // Use native ES modules
    "moduleResolution": "NodeNext",      // Proper resolution for ESM in Node
    "lib": ["ES2022"],                   // Standard library features

    /* Paths and Output */
    "rootDir": "src",                    // Source files live here
    "outDir": "dist",                    // Compiled output goes here
    "baseUrl": "./src",                  // Simplify imports with absolute paths

    /* Type Safety */
    "strict": true,                      // Enable all strict type checks
    "noImplicitAny": true,               // Prevent implicit `any`
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,                // Faster builds, skip type checking of node_modules

    /* Interop */
    "esModuleInterop": true,             // Allow default imports from CommonJS
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,           // Import JSON files directly

    /* Node Types */
    "types": ["node"],                   // Include Node.js type definitions

    /* Source Maps for Debugging */
    "sourceMap": true
  },
  "include": ["src"],                    // Compile everything in src/
  "exclude": ["node_modules", "dist"]    // Ignore build output and deps
}
